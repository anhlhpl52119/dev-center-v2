# Rule add new SVG

- Replace `fill="..."` or `stroke="..."` with some fixed color

to  `fill="currentColor"` of `stroke="currentColor"` this will apply custom style of color for svg icon

- Then cleanup and restart nuxt

```bash
npx nuxt cleanup
npm run dev
```
